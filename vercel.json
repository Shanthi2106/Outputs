{
  "version": 2,
  "buildCommand": "echo 'Building backend...' && cd backend && npm install && npm run build && echo 'Backend built successfully' && cd ../frontend && echo 'Building frontend...' && npm install && npm run build && echo 'Frontend built successfully'",
  "outputDirectory": "frontend/dist",
  "installCommand": "echo 'Installation handled in buildCommand'",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "functions": {
    "api/index.js": {
      "runtime": "nodejs20.x"
    }
  }
}
