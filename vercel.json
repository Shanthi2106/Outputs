{
  "version": 2,
  "buildCommand": "PROJECT_ROOT=$(find . -name 'vercel.json' -type f | head -1 | xargs dirname) && cd \"$PROJECT_ROOT\" && (cd backend && npm install && npm run build) && (cd frontend && npm install && npm run build)",
  "outputDirectory": "frontend/dist",
  "installCommand": "echo 'Dependencies installed during build'",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "functions": {
    "api/index.js": {
      "runtime": "nodejs20.x"
    }
  }
}
